export default function CoProductsWebsite() {
  return (
    <div className="bg-white text-gray-800 font-sans">
      <header className="bg-green-600 text-white p-6 shadow-md">
        <h1 className="text-3xl font-bold">Co-Products & Waste Management</h1>
        <p className="text-sm mt-1">Sustainable pathways for a circular future</p>
      </header>

      <main className="p-6 space-y-16">
        {/* Home Section */}
        <section id="home">
          <h2 className="text-2xl font-semibold mb-2">Welcome</h2>
          <p>
            This platform explores innovative strategies for utilizing co-products and reducing waste,
            particularly in the brewing industry and beyond.
          </p>
        </section>

        {/* About Section */}
        <section id="about">
          <h2 className="text-2xl font-semibold mb-2">About the Topic</h2>
          <p>
            Co-products such as spent grains, surplus yeast, and trub are valuable resources that can
            be transformed into animal feed, energy, or bio-based materials. Effective waste management
            is crucial for environmental and economic sustainability.
          </p>
        </section>

        {/* Projects Section */}
        <section id="projects">
          <h2 className="text-2xl font-semibold mb-2">Case Studies & Valorization Pathways</h2>
          <ul className="list-disc list-inside space-y-1">
            <li>Spent grain valorization for energy production (biogas, pellets)</li>
            <li>Surplus yeast as a protein source in animal feed</li>
            <li>CO₂ capture and reuse in the brewing process</li>
            <li>Trub reuse in composting or bioplastics</li>
          </ul>
        </section>

        {/* Contact Section */}
        <section id="contact">
          <h2 className="text-2xl font-semibold mb-2">Get in Touch</h2>
          <p>Have a question or collaboration idea? Reach out:</p>
          <form className="mt-4 space-y-4 max-w-md">
            <input
              type="text"
              placeholder="Your Name"
              className="w-full border p-2 rounded"
            />
            <input
              type="email"
              placeholder="Your Email"
              className="w-full border p-2 rounded"
            />
            <textarea
              placeholder="Your Message"
              className="w-full border p-2 rounded h-24"
            ></textarea>
            <button className="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition">
              Send
            </button>
          </form>
        </section>
      </main>

      <footer className="bg-gray-100 text-center p-4 text-sm text-gray-500 mt-10">
        &copy; 2025 Co-Products Platform – Created by Alejandro
      </footer>
    </div>
  );
}
